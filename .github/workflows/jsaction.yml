name: jsaction

on:
  workflow_dispatch:

jobs:
  test-js-action:
    name: job to test our JS action
    runs-on: ubuntu-latest
    steps:
      - uses: ./
        with:
          number1: 4
          number2: 2